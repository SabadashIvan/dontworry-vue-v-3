# Application Environment
# Options: local | staging | prod
VITE_APP_ENV=local

# API Configuration
# HTTP scheme for API requests (http for local, https for production)
VITE_API_SCHEME=http

# Backend API port (for local development, not used in production)
VITE_API_PORT=8000

# API path prefix (usually /v1)
VITE_API_PATH_PREFIX=/v1

# Central Domain (optional, for local development)
# If set, central API calls will use this host instead of current hostname
# For local: app.dontworry.cloud
# For production: leave empty or set to your production central domain
VITE_CENTRAL_HOST=app.dontworry.cloud

# Central Domains (comma-separated list)
# Used to determine if current hostname is central or tenant
# If hostname matches one of these, app runs in "central" mode
# Otherwise, app runs in "tenant" mode (expects subdomain)
# 
# Local development:
VITE_CENTRAL_DOMAINS=dontworry.cloud,app.dontworry.cloud
# 
# Production example (uncomment and adjust):
# VITE_CENTRAL_DOMAINS=dontworry.com,app.dontworry.com

# Backend API Base URL (optional, for production)
# If set, this URL will be used for central API calls instead of dynamic construction
# Should include scheme and host, path prefix will be appended automatically
# 
# For local development: leave empty (uses dynamic construction with VITE_API_PORT)
# For production: set to your API domain
# Example: https://api.dontworry.com
# 
# Note: For tenant mode, URL is always constructed dynamically using current subdomain
# VITE_API_BASE_URL=
